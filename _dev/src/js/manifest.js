// 'use strict';

jQuery(document).ready(function($) {
  require('./video-ajax');
  require('./video-media-library');
  require('./video-field-updated');
  require('./video-clear');

});

window.$ = jQuery;
